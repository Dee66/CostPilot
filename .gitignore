# Distribution archives (build locally, don't commit)
/dist
/dist-v*/
dist/
**/dist/
dist/costpilot-*.zip
dist/costpilot-*.tar.gz
*.tar.gz
*.zip
sha256sum.txt
SHA256SUMS.txt
**/RELEASE_DESCRIPTION.md

# Pro Engine artifacts
dist/pro-engine-*
premium/engine_build/*.wasm

# Packaging temporary files
/build/package/tmp
/staging/core/

# Build artifacts
build/artifacts/
**/build_artifacts/

# Signing keys (private key only - public key IS committed)
packaging/signing/private.key
scripts/signing/private.key
**/private.key
**/rotated_key.pem
*.pem
!scripts/test-data/*.pem

# Internal planning docs (never commit)
docs/mental_model_delta*
docs/planning/
docs/API_PRIVATE_KEY_FIX.md

# Rust build artifacts
/target

# Cargo.lock for libraries (keep for binaries)
# Cargo.lock

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Local runtime state and caches (never commit these)
.costpilot/
.benchmarks/
.pytest_cache/
.scanner/
.guardsuite/
scalability-test-results/
.archive/

# Build and test artifacts (never commit these)
build/artifacts/
**/build_artifacts/

# Release artifact directories
linux-amd64/
dist-v*/
dist/

# Private signing keys (NEVER commit these)
scripts/signing/private.key
scripts/cleanup/build_artifacts/build/keys/rotated_key.pem
scripts/cleanup/build_artifacts/build/keys/*.pem
**/private*.key
**/rotated*.pem

# Test outputs
*.profdata
*.profraw

# Debug artifacts
*.dSYM
tmp.txt

# Test keys and licenses
test_*.pem
test_license.json
.costpilot/audit_log.json

# Master license keys (NEVER commit private keys)
*.pem
!**/test_fixtures/**/*.pem
license_customer.json

# Test data and temporary files
test_*.json
test_*.log
.archive/

# Generated artifacts
costpilot
trend.html
trend.svg

# Internal reports and analysis (don't commit to public repo)
reports/
repos.txt
route53-policy.json

# Test results and generated data (don't commit to public repo)
docs/results/

# Large Rust build artifacts
target/

# Node.js dependencies (if any)
node_modules/
package-lock.json

# Python virtual environments
.venv/
venv/
ENV/
env/

# Python cache files (never commit these)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Large log files
*.log

# Copilot
AGENTS.md
docs/mental_model.md
.github/copilot-instructions.md

# License files
license*.json

docs/checklist.md
docs/checklist.yml
docs/pricing_enforcement_checklist.yml
keypair_info.json
dist-v1.0.1/
